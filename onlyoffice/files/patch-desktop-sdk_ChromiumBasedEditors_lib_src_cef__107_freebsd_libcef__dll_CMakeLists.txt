--- desktop-sdk/ChromiumBasedEditors/lib/src/cef_107/freebsd/libcef_dll/CMakeLists.txt.orig	2025-09-24 18:05:29 UTC
+++ desktop-sdk/ChromiumBasedEditors/lib/src/cef_107/freebsd/libcef_dll/CMakeLists.txt
@@ -10,6 +10,9 @@ macro(LIBCEF_APPEND_PLATFORM_SOURCES name_of_list)
   if("${CMAKE_SYSTEM_NAME}" STREQUAL "Linux" AND ${name_of_list}_LINUX)
     list(APPEND ${name_of_list} ${${name_of_list}_LINUX})
   endif()
+  if("${CMAKE_SYSTEM_NAME}" STREQUAL "FreeBSD" AND ${name_of_list}_FREEBSD)
+    list(APPEND ${name_of_list} ${${name_of_list}_LINUX})
+  endif()
   if("${CMAKE_SYSTEM_NAME}" STREQUAL "Windows" AND ${name_of_list}_WINDOWS)
     list(APPEND ${name_of_list} ${${name_of_list}_WINDOWS})
   endif()
@@ -666,6 +669,10 @@ set(LIBCEF_INCLUDE_INTERNAL_SRCS_LINUX
   )
 set(LIBCEF_INCLUDE_INTERNAL_SRCS_LINUX
   ../include/internal/cef_linux.h
+  ../include/internal/cef_types_linux.h
+  )
+set(LIBCEF_INCLUDE_INTERNAL_SRCS_FREEBSD
+  ../include/internal/cef_freebsd.h
   ../include/internal/cef_types_linux.h
   )
 set(LIBCEF_INCLUDE_INTERNAL_SRCS_MAC
