--- physx/buildtools/packman/packman.orig	2024-05-31 15:16:16 UTC
+++ physx/buildtools/packman/packman
@@ -13,6 +13,8 @@
 # WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 # See the License for the specific language governing permissions and
 # limitations under the License.
+PM_PYTHON_EXT="python3"
+PM_PYTHON="python3"
 SAVED_SETTINGS="$-"
 set -eu
 
@@ -95,6 +97,8 @@ install_python()
 		PYTHON_PACKAGE=$PYTHON_VERSION-linux-x86_64
 	elif [ $PLATFORM == 'Linux' ] && [ $PROCESSOR == 'aarch64' ]; then
 		PYTHON_PACKAGE=$PYTHON_VERSION-linux-aarch64
+	elif [ $PLATFORM == 'FreeBSD' ] && [ $PROCESSOR == 'amd64' ]; then
+                PYTHON_PACKAGE=$
 	else
 		echo "Operating system not supported"
 		exit 1
@@ -123,9 +127,8 @@ install_python()
 
 # Ensure python is available:
 if [ -z "${PM_PYTHON_EXT:-}" ]; then
-	install_python
-else
-	PM_PYTHON="$PM_PYTHON_EXT"
+#	install_python
+	PM_PYTHON=python3
 fi
 
 # The packman module may be externally configured
